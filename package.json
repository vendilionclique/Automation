{
  "name": "taobao-mtg-visual-collector",
  "version": "0.1.0",
  "private": true,
  "description": "Local visual collection tooling spike for Taobao MTG price evidence.",
  "type": "module",
  "scripts": {
    "midscene:version": "node -e \"const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('node_modules/@midscene/computer/package.json','utf8')); console.log(pkg.name, pkg.version);\"",
    "midscene:computer:help": "midscene-computer --help"
  },
  "devDependencies": {
    "@midscene/computer": "^1.7.10"
  }
}
